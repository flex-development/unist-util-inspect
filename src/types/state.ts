/**
 * @file Type Aliases - State
 * @module unist-util-inspect/types/State
 */

/**
 * Info passed around.
 *
 * @internal
 */
type State = {
  /**
   * Boolean indicating if positional information should be included.
   */
  positions: boolean
}

export type { State as default }
